// The extension has its own version line, independent of the Tab Space app
// version. Both the build script and the source packaging script read it here
// so a release only has to bump one number.
export const extensionVersion = "1.2.0"
